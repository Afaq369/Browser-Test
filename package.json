{
  "name": "browser-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "chrome": "npx playwright test --headed --browser=chromium",
    "firefox": "npx playwright test --headed --browser=firefox",
    "safari": "npx playwright test --headed --browser=webkit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.44.1"
  }
}
